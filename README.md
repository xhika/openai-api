# openai-api
 Access OpenAI API with php.

```bash
	copy .env.example as .env and enter your api key
	use prefered engine, davinci is set as default
	use prompt.txt in /src folder to enter your text
```


